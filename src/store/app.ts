import { defineStore } from 'pinia'

// các thông tin dùng chung
export const useApp = defineStore('app', {
    state: () => {
        return {
            options: {
                
            }
        }
    }
})